sudo: false

language: python

python:
  - 2.7

services:
    - postgresql

#install dependencies
install: pip install -qr vagrant/tradyfit/requirements.txt

#create database
before_script:
  - postgres createuser -dRS vagrant
  - psql -c 'create database tradyfit_test;' -U vagrant

#run tests
script: python vagrant/tradyfit/manage.py test

